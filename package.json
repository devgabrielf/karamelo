{
	"name": "karamelo",
	"version": "1.0.0",
	"private": false,
	"license": "MIT",
	"scripts": {
		"install": "npm --prefix backend i && npm --prefix frontend i",
		"backend": "npm --prefix backend run db:dev:up && npm --prefix backend run start:dev",
		"frontend": "npm --prefix frontend run dev"
	}
}
